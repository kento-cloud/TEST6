# Plan.md

## プロジェクト概要

このプロジェクトは、PIVOTのような高品質なビジネスメディアUIを参考にしながら、見た目・操作感・情報設計を限りなく高精度で再現したローカル環境用フロントエンドを構築することを目的とする。

単なる「PIVOT風サイト」を作ることは目的ではない。

本プロジェクトでは、トップページ・動画ページ・ランキング・カテゴリ・検索・プレイリスト・マイリストなどを含め、実際のメディアサービスとして成立するレベルのUI/UXを研究・再構築していく。

また、最終的には動画アップロードを起点として、AIによる文字起こし・要約・チャプター生成・記事生成を行う「AIネイティブなメディアCMS」へ発展させることを前提としている。

ただし、現時点ではAI機能の実装を優先しない。 最優先事項は、UIと操作感の高精度な再現である。

## 最終ゴール

最終的には、以下を実現する。

- 動画を投稿できる
- AIが動画を自動文字起こしする
- AIが動画内容をセクション単位で要約する
- AIがチャプターを自動生成する
- AIが記事化を行う
- AIがタグ・カテゴリ・関連動画を生成する
- 動画・記事・ランキング・検索が連動したメディアサイトとして成立する
- 管理画面から投稿・編集・確認ができる

ただし、その前提として「UIの完成度」が必要になる。

現在のフェーズでは、PIVOTクラスのUI/UX品質を持つフロントエンドを完成させることを最優先とする。

## 現在のフェーズ

現在は「UI Calibration Phase（UI差分調整フェーズ）」である。

このフェーズでは、新機能追加よりも、本家との差分を減らすことを優先する。

具体的には以下を行う。

- フォントサイズの一致
- 余白の一致
- グラデーションの一致
- カード比率の一致
- スライダー速度の一致
- インジケーターの一致
- ホバー挙動の一致
- レスポンシブ挙動の一致
- ヘッダー透明度の一致
- ボタン角丸の一致
- レイアウト密度の一致

「それっぽい」ではなく、「本家との差分を減らす」ことを目的とする。

## 優先順位

現在の優先順位は以下。

1. トップページのUI完全度向上
2. スライダー・インジケーター・hoverなどの動作一致
3. レスポンシブ一致
4. 内部ページUIの一致
5. ローカルルーティング整備
6. 記事データ構造化
7. AI連携準備
8. 管理画面
9. AI機能実装

機能追加よりも、UI差分修正を優先する。

## 現在の重要方針

### 1. 「PIVOT風」を作らない

目的は「PIVOT風サイト」を作ることではない。

目的は、PIVOTレベルの完成度を持つUI/UXを研究・再現し、その上で将来的なAIメディアCMSへ発展させることである。

そのため、デザインを勝手にアレンジしない。

### 2. 改善しない

「もっと良いUI」に変更しない。

今必要なのは改善ではなく、本家との差分を減らすことである。

### 3. 一括修正しない

UI修正時は、一度に複数箇所を変更しない。

必ず以下を守る。

- 1回につき1項目だけ修正
- 修正前後で比較
- 差分が改善した場合のみ採用
- 他要素を巻き込まない

### 4. 本家リンクへ遷移しない

内部リンクは必ずローカルルーティングを使用する。

クリック時に本家URLへ遷移してはいけない。

### 5. placeholderで誤魔化さない

雑な仮UIを作らない。

最低限のダミーデータは許可するが、「後で直す前提の雑な構造」を増やさない。

## UIに関する考え方

このプロジェクトでは、単なるWeb制作ではなく、「ブラウザUIの高精度再現」に近い考え方を採用する。

重要なのは以下。

- px単位の余白
- opacity
- line-height
- card ratio
- font-weight
- easing
- transition
- responsive density
- interaction timing

感覚で調整するのではなく、本家との差分を減らしていく。

## 将来追加する機能

UI完成後に、以下を段階的に追加する。

- **動画アップロード**: 動画投稿、サムネイル設定、カテゴリ設定、タイトル設定
- **AI文字起こし**: Whisper等による自動文字起こし、字幕生成
- **AIチャプター生成**: セクション分割、タイトル生成、時間範囲生成
- **AI要約**: ダイジェスト生成、要点抽出、見出し生成
- **AI記事生成**: 動画内容から記事生成、SEO向け構造化、要約記事生成
- **AI推薦**: 関連動画、タグ、ランキング、カテゴリ推薦

## 現在やらないこと

以下はまだ優先しない。

- SEO最適化
- 本番公開
- 会員課金
- 外部決済
- 本格認証
- サーバー負荷対策
- DB最適化
- AIエージェント化

まずはUI/UX品質を優先する。

---

# テスト要件定義（原案） — 2026-05-16 追加

## 1. サービス概要

* 自社がアップロードする動画を中心としたウェブサービス。
* UIはYouTube風で、動画の一覧・閲覧・検索が容易。
* 動画自体はユーザーがアップロードする必要はなく、管理者側で一括アップロード。

---

## 2. 動画表示

* サムネイル表示：動画の代表画像を表示。
* タイトル表示：動画タイトルを表示。
* 概要欄表示：動画の詳細説明文。
* 文章表示オプション：
    * 動画を再生せずに「文章で内容を確認」可能。
    * AI生成による文章要約・リライトで、人間らしい自然な文章として表示。

---

## 3. AI生成コンテンツ

* 生成対象：
    * 概要欄の文章
    * 「文章で見る」用コンテンツ
* 生成方針：
    * AI感を極力抑え、人間らしい自然な文章。
    * プロンプト設計によってトーン・表現を統一。
    * 過去のサムネイル・文章パターンを参照して統一感を保つ。

---

## 4. サムネイル生成と統一

* デザインテンプレート選択：
    * CMS上でテンプレート（A/B/C/D/F/G等）を選択。
    * 選択したテンプレートに沿ったサムネイルがAI生成される。
* 統一性の担保：
    * カラーリング・フォント・構図などはテンプレートに依存。
    * 毎回の生成でも一貫性を保持。
* 一括生成：
    * 1回の操作で5枚を生成し、ベストなものを選択可能。

---

## 5. CMS機能

* 動画アップロード管理
* タイトル・概要欄入力
* AI生成文章の確認・修正
* サムネイル生成テンプレート選択
* 生成結果のプレビュー・公開
* 過去生成履歴の参照

---

## 6. プロンプト設計方針

* 各テンプレートごとにプロンプトを用意。
* プロンプトにより以下を制御：
    * 文体・トーン
    * デザイン要素（カラー、構図、テキスト配置）
    * 人間らしさの維持
* CMS上でテンプレートを選択するだけでプロンプトが自動適用される。

---

## 7. 将来的な拡張

* ユーザーが自分でテンプレートを追加できる仕組み。
* AI生成文章の多言語対応。
* 動画以外のコンテンツ（画像・PDF等）のAI要約対応。

---

## 実装ステータス（2026-05-16時点）

| # | 要件 | ステータス |
|---|------|-----------|
| 1 | サービス概要 | ✅ 完了 |
| 2 | 動画表示 | ✅ 完了 |
| 3 | AI生成コンテンツ（人間らしさ） | ✅ 完了 |
| 4 | サムネイル生成（5枚一括+テンプレート） | ✅ 完了 |
| 5 | CMS機能（履歴参照含む） | ✅ 完了 |
| 6 | プロンプト設計方針（テンプレート自動適用） | ✅ 完了 |
| 7 | 将来的な拡張 | 📋 未着手（対象外） |
